---
phase: 06-ai-tools-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tools/ai-insights.tsx
  - app/tools/ai-search.tsx
  - app/tools/smart-reminders.tsx
  - app/tools/_layout.tsx
  - components/AiInsightCard.tsx
  - hooks/useAiNudge.ts
  - supabase/functions/ai-insights/index.ts
  - supabase/functions/ai-search/index.ts
  - supabase/functions/ai-draft-reminders/index.ts
  - app/(tabs)/tools/index.tsx
  - app/(tabs)/dashboard/index.tsx
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "AI Insights, Smart Reminders, and AI Search do not appear in the tools menu"
    - "Tools menu shows Payment History, Expenses, and three Coming Soon placeholders (Documents, Maintenance, Analytics)"
    - "Tapping a Coming Soon card shows a toast, not a navigation"
    - "Dashboard renders without AiInsightCard or useAiNudge references"
    - "npx tsc --noEmit passes with zero errors"
    - "No ai-insights, ai-search, or ai-draft-reminders directories exist under supabase/functions/"
  artifacts:
    - path: "app/(tabs)/tools/index.tsx"
      provides: "Updated tools menu with Coming Soon cards"
      contains: "comingSoon"
    - path: "app/(tabs)/dashboard/index.tsx"
      provides: "Dashboard without AI references"
  key_links:
    - from: "app/(tabs)/tools/index.tsx"
      to: "Coming Soon toast"
      via: "onPress handler shows Snackbar/toast instead of router.push"
      pattern: "Coming soon"
---

<objective>
Delete all deprecated AI tool screens, components, hooks, and Edge Functions. Update the tools menu to replace AI entries with Coming Soon placeholders and clean AI references from the dashboard.

Purpose: Free navigation slots and remove dead code before adding Document Storage, Maintenance, and Analytics features in subsequent phases.
Output: Clean tools menu with active items + Coming Soon cards, dashboard without AI card, no stale backend functions.
</objective>

<execution_context>
@C:/Users/Spongeass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Spongeass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-tools-removal/06-CONTEXT.md

<interfaces>
<!-- Current tools menu pattern from app/(tabs)/tools/index.tsx -->
```typescript
const TOOLS = [
  {
    label: 'Payment History',
    description: 'Track rent payments and confirmations',
    icon: 'receipt',
    route: '/payments',
    color: colors.primary,
  },
  // ... more entries
] as const;
```

<!-- Dashboard AI imports to remove from app/(tabs)/dashboard/index.tsx -->
```typescript
import { useAiNudge } from '@/hooks/useAiNudge';
import { AiInsightCard } from '@/components/AiInsightCard';
// Usage:
const { nudge: aiNudge, loading: aiNudgeLoading } = useAiNudge(user?.id);
// JSX:
<AiInsightCard nudge={aiNudge} loading={aiNudgeLoading} />
```

<!-- Theme hook provides colors for styling -->
```typescript
const { colors, shadows } = useTheme();
// colors.textDisabled, colors.textSecondary available for muted styling
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete AI screen files, components, hooks, and Edge Functions</name>
  <files>
    app/tools/ai-insights.tsx,
    app/tools/ai-search.tsx,
    app/tools/smart-reminders.tsx,
    app/tools/_layout.tsx,
    components/AiInsightCard.tsx,
    hooks/useAiNudge.ts,
    supabase/functions/ai-insights/index.ts,
    supabase/functions/ai-search/index.ts,
    supabase/functions/ai-draft-reminders/index.ts
  </files>
  <read_first>
    app/tools/ai-insights.tsx,
    app/tools/ai-search.tsx,
    app/tools/smart-reminders.tsx,
    app/tools/_layout.tsx,
    components/AiInsightCard.tsx,
    hooks/useAiNudge.ts
  </read_first>
  <action>
Delete the following files entirely (use rm or equivalent):

**Screen files (3):**
- `app/tools/ai-insights.tsx`
- `app/tools/ai-search.tsx`
- `app/tools/smart-reminders.tsx`

**Layout file (1):**
- `app/tools/_layout.tsx` — After removing all 3 AI screens, no routes remain under `app/tools/`. Delete the layout to avoid an empty Stack navigator. The tools tab entry point lives at `app/(tabs)/tools/index.tsx` and is unaffected.

**Component file (1):**
- `components/AiInsightCard.tsx`

**Hook file (1):**
- `hooks/useAiNudge.ts`

**Edge Function directories (3 entire directories, not just index.ts):**
- `supabase/functions/ai-insights/` (entire directory)
- `supabase/functions/ai-search/` (entire directory)
- `supabase/functions/ai-draft-reminders/` (entire directory)

After deletion, verify no other files import from these deleted modules by running a grep for the deleted paths. If any unexpected imports are found, note them for Task 2.

NOTE: Deployed Edge Functions must be manually removed from the Supabase dashboard — this cannot be automated via code. The executor should log this as a post-plan manual step.
  </action>
  <verify>
    <automated>ls app/tools/ai-insights.tsx app/tools/ai-search.tsx app/tools/smart-reminders.tsx app/tools/_layout.tsx components/AiInsightCard.tsx hooks/useAiNudge.ts supabase/functions/ai-insights/index.ts supabase/functions/ai-search/index.ts supabase/functions/ai-draft-reminders/index.ts 2>&1 | grep -c "No such file"</automated>
  </verify>
  <acceptance_criteria>
    - `app/tools/ai-insights.tsx` does not exist
    - `app/tools/ai-search.tsx` does not exist
    - `app/tools/smart-reminders.tsx` does not exist
    - `app/tools/_layout.tsx` does not exist
    - `components/AiInsightCard.tsx` does not exist
    - `hooks/useAiNudge.ts` does not exist
    - `supabase/functions/ai-insights/` directory does not exist
    - `supabase/functions/ai-search/` directory does not exist
    - `supabase/functions/ai-draft-reminders/` directory does not exist
    - `grep -r "ai-insights\|ai-search\|ai-draft-reminders\|AiInsightCard\|useAiNudge" app/ components/ hooks/ --include="*.tsx" --include="*.ts" -l` returns no results (no stale imports remain)
  </acceptance_criteria>
  <done>All 9 AI-related files/directories are deleted. No remaining imports reference them anywhere in the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Update tools menu and dashboard to remove AI references</name>
  <files>
    app/(tabs)/tools/index.tsx,
    app/(tabs)/dashboard/index.tsx
  </files>
  <read_first>
    app/(tabs)/tools/index.tsx,
    app/(tabs)/dashboard/index.tsx
  </read_first>
  <action>
**A. Update `app/(tabs)/tools/index.tsx`:**

1. Add `comingSoon?: boolean` to the tool item type by converting the TOOLS array from `as const` to a typed array. Each item has shape: `{ label: string; description: string; icon: string; route?: string; color: string; comingSoon?: boolean }`.

2. Remove the 3 AI entries (AI Insights, Smart Reminders, AI Search).

3. Keep the 2 active entries at the top:
   - Payment History (`icon: 'receipt'`, `route: '/payments'`, `color: colors.primary`)
   - Expenses (`icon: 'cash-minus'`, `route: '/expenses'`, `color: colors.statusPartial`)

4. Add 3 Coming Soon entries after the active items:
   ```typescript
   {
     label: 'Documents',
     description: 'Upload and manage property documents',
     icon: 'file-document-outline',
     color: '#6366F1',
     comingSoon: true,
   },
   {
     label: 'Maintenance',
     description: 'Track and manage maintenance requests',
     icon: 'wrench-outline',
     color: '#14B8A6',
     comingSoon: true,
   },
   {
     label: 'Analytics',
     description: 'Financial reports and insights',
     icon: 'chart-bar',
     color: '#F97316',
     comingSoon: true,
   },
   ```

5. Update the rendering logic: for items where `comingSoon` is true:
   - Set `opacity: 0.5` on the card
   - Replace `onPress` with a toast/Snackbar: `useToastStore.getState().showToast('Coming soon!', 'info')` (import `useToastStore` from `@/lib/toast`)
   - Show a small "COMING SOON" badge text to the right of the card title (fontSize 9, fontWeight '700', color `colors.textDisabled`, backgroundColor `colors.surfaceElevated`, paddingHorizontal 6, paddingVertical 2, borderRadius 4, marginLeft 8, overflow 'hidden')
   - Hide the chevron-right icon for Coming Soon items

6. Remove `type Href` from the `expo-router` import since Coming Soon items have no route. Use `as Href` cast inline on the `router.push()` call for active items, or keep the import if cleaner.

**B. Update `app/(tabs)/dashboard/index.tsx`:**

1. Remove these two import lines:
   - `import { useAiNudge } from '@/hooks/useAiNudge';` (line 18)
   - `import { AiInsightCard } from '@/components/AiInsightCard';` (line 23)

2. Remove the `useAiNudge` hook call:
   - Delete: `const { nudge: aiNudge, loading: aiNudgeLoading } = useAiNudge(user?.id);` (line 100)

3. Remove the AiInsightCard JSX usage:
   - Delete: `<AiInsightCard nudge={aiNudge} loading={aiNudgeLoading} />` (line 435)
   - The surrounding elements (GradientButton above and SetupPromptModal below) remain — no gap-filling needed.

4. Run `npx tsc --noEmit` to confirm zero TypeScript errors after all changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/(tabs)/tools/index.tsx` contains `label: 'Documents'` and `label: 'Maintenance'` and `label: 'Analytics'`
    - `app/(tabs)/tools/index.tsx` contains `comingSoon: true` (at least 3 occurrences)
    - `app/(tabs)/tools/index.tsx` contains `Coming soon!` string for toast
    - `app/(tabs)/tools/index.tsx` does NOT contain `AI Insights` or `Smart Reminders` or `AI Search`
    - `app/(tabs)/dashboard/index.tsx` does NOT contain `useAiNudge` or `AiInsightCard`
    - `npx tsc --noEmit` exits with code 0 (zero errors)
  </acceptance_criteria>
  <done>Tools menu shows Payment History + Expenses as active items, followed by Documents + Maintenance + Analytics as visually muted Coming Soon cards with toast on tap. Dashboard renders cleanly without any AI component references. TypeScript compiles with zero errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `grep -r "ai-insights\|ai-search\|ai-draft-reminders\|AiInsightCard\|useAiNudge" app/ components/ hooks/ --include="*.tsx" --include="*.ts" -l` — returns nothing
3. `ls supabase/functions/ | grep ai` — returns nothing
4. `grep "comingSoon" app/\(tabs\)/tools/index.tsx | wc -l` — returns 3 or more
</verification>

<success_criteria>
- All 3 AI screen files deleted
- All 3 AI Edge Function directories deleted
- AiInsightCard component and useAiNudge hook deleted
- Tools layout file deleted (empty after screen removal)
- Tools menu has 2 active items + 3 Coming Soon placeholders
- Coming Soon cards are visually muted (opacity 0.5) with "COMING SOON" badge
- Coming Soon cards show toast on tap, not navigation
- Dashboard has no AI imports or AI component rendering
- TypeScript compiles cleanly (npx tsc --noEmit passes)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-tools-removal/06-01-SUMMARY.md`
</output>
