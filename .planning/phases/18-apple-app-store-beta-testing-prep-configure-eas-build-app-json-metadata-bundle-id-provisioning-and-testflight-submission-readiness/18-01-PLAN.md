---
phase: 18-apple-app-store-beta-testing-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - eas.json
  - components/UpdateGate.tsx
autonomous: true
requirements:
  - EAS-01
  - EAS-02
  - EAS-03
  - EAS-04
  - EAS-05
  - EAS-06
  - EAS-07

must_haves:
  truths:
    - "eas.json submit profile contains the App Store Connect App ID so eas submit runs non-interactively"
    - "eas.json production env contains PostHog API key and host so analytics works in TestFlight builds"
    - "app.json iOS section declares Apple Sign In capability so production builds enable the entitlement"
    - "app.json iOS section contains the Apple Team ID so EAS Build does not prompt interactively"
    - "app.json splash background is brand indigo (#4F46E5), not teal (#009688)"
    - "UpdateGate.tsx contains the real App Store numeric ID, not a placeholder"
    - "app.json iOS section declares privacy manifests for UserDefaults API usage"
  artifacts:
    - path: "eas.json"
      provides: "Submit profile with ascAppId, production env with PostHog vars"
      contains: "ascAppId"
    - path: "app.json"
      provides: "iOS capabilities, team ID, privacy manifests, splash color"
      contains: "usesAppleSignIn"
    - path: "components/UpdateGate.tsx"
      provides: "Real App Store URL for update gate"
      contains: "id6760478576"
  key_links:
    - from: "eas.json"
      to: "App Store Connect"
      via: "ascAppId field in submit.production.ios"
      pattern: "ascAppId.*6760478576"
    - from: "app.json"
      to: "EAS Build capability sync"
      via: "usesAppleSignIn and appleTeamId fields"
      pattern: "usesAppleSignIn.*true"
---

<objective>
Fix all configuration gaps in app.json, eas.json, and UpdateGate.tsx so the Expo app is ready for a clean EAS production build and TestFlight submission.

Purpose: Close every known config issue identified in research -- missing submit profile, missing env vars, missing iOS capabilities, wrong splash color, placeholder App Store ID, and missing privacy manifest. All seven EAS requirements (01-07) are pure config edits that must be correct before triggering the cloud build.

Output: Three files updated (app.json, eas.json, components/UpdateGate.tsx) passing all seven config validation checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-apple-app-store-beta-testing-prep-configure-eas-build-app-json-metadata-bundle-id-provisioning-and-testflight-submission-readiness/18-RESEARCH.md
@app.json
@eas.json
@components/UpdateGate.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app.json and eas.json with all required config</name>
  <files>app.json, eas.json</files>
  <read_first>app.json, eas.json, .planning/phases/18-apple-app-store-beta-testing-prep-configure-eas-build-app-json-metadata-bundle-id-provisioning-and-testflight-submission-readiness/18-RESEARCH.md</read_first>
  <action>
Edit app.json — make these changes to the `expo` object:

1. (EAS-05) Change `splash.backgroundColor` from `"#009688"` to `"#4F46E5"`

2. (EAS-03) Add `"usesAppleSignIn": true` to the `ios` object (after `bundleIdentifier`)

3. (EAS-04) Add `"appleTeamId": "35G5HXD9K9"` to the `ios` object (after `bundleIdentifier`)

4. (EAS-07) Add `"privacyManifests"` to the `ios` object with this exact structure:
```json
"privacyManifests": {
  "NSPrivacyAccessedAPITypes": [
    {
      "NSPrivacyAccessedAPIType": "NSPrivacyAccessedAPICategoryUserDefaults",
      "NSPrivacyAccessedAPITypeReasons": ["CA92.1"]
    }
  ]
}
```

The final `ios` section should look like:
```json
"ios": {
  "supportsTablet": true,
  "bundleIdentifier": "com.dwella.app",
  "appleTeamId": "35G5HXD9K9",
  "usesAppleSignIn": true,
  "infoPlist": {
    "ITSAppUsesNonExemptEncryption": false
  },
  "privacyManifests": {
    "NSPrivacyAccessedAPITypes": [
      {
        "NSPrivacyAccessedAPIType": "NSPrivacyAccessedAPICategoryUserDefaults",
        "NSPrivacyAccessedAPITypeReasons": ["CA92.1"]
      }
    ]
  }
}
```

Edit eas.json — make these changes:

5. (EAS-02) Add two env vars to `build.production.env`:
   - `"EXPO_PUBLIC_POSTHOG_API_KEY": "phc_gn0yjefWBNNwymzZhPQ4qkUAngg7xYvLVpAJ6gmEiNc"`
   - `"EXPO_PUBLIC_POSTHOG_HOST": "https://eu.i.posthog.com"`

6. (EAS-01) Replace the empty `"production": {}` under `submit` with:
```json
"production": {
  "ios": {
    "ascAppId": "6760478576"
  }
}
```

Do NOT change any other fields. Preserve all existing values.
  </action>
  <verify>
    <automated>node -e "const e=require('./eas.json'),a=require('./app.json'); console.assert(e.submit.production.ios.ascAppId==='6760478576','EAS-01 fail'); console.assert(e.build.production.env.EXPO_PUBLIC_POSTHOG_API_KEY==='phc_gn0yjefWBNNwymzZhPQ4qkUAngg7xYvLVpAJ6gmEiNc','EAS-02 fail'); console.assert(e.build.production.env.EXPO_PUBLIC_POSTHOG_HOST==='https://eu.i.posthog.com','EAS-02b fail'); console.assert(a.expo.ios.usesAppleSignIn===true,'EAS-03 fail'); console.assert(a.expo.ios.appleTeamId==='35G5HXD9K9','EAS-04 fail'); console.assert(a.expo.splash.backgroundColor==='#4F46E5','EAS-05 fail'); console.assert(!!a.expo.ios.privacyManifests,'EAS-07 fail'); console.assert(a.expo.ios.privacyManifests.NSPrivacyAccessedAPITypes[0].NSPrivacyAccessedAPIType==='NSPrivacyAccessedAPICategoryUserDefaults','EAS-07b fail'); console.log('All config checks passed')"</automated>
  </verify>
  <acceptance_criteria>
    - app.json `expo.splash.backgroundColor` equals `"#4F46E5"`
    - app.json `expo.ios.usesAppleSignIn` equals `true`
    - app.json `expo.ios.appleTeamId` equals `"35G5HXD9K9"`
    - app.json `expo.ios.privacyManifests.NSPrivacyAccessedAPITypes[0].NSPrivacyAccessedAPIType` equals `"NSPrivacyAccessedAPICategoryUserDefaults"`
    - eas.json `submit.production.ios.ascAppId` equals `"6760478576"`
    - eas.json `build.production.env.EXPO_PUBLIC_POSTHOG_API_KEY` equals `"phc_gn0yjefWBNNwymzZhPQ4qkUAngg7xYvLVpAJ6gmEiNc"`
    - eas.json `build.production.env.EXPO_PUBLIC_POSTHOG_HOST` equals `"https://eu.i.posthog.com"`
    - No other fields in app.json or eas.json are changed
  </acceptance_criteria>
  <done>All seven config values are set correctly. The automated node assertion script exits 0 with "All config checks passed".</done>
</task>

<task type="auto">
  <name>Task 2: Replace [APP_ID] placeholder in UpdateGate.tsx</name>
  <files>components/UpdateGate.tsx</files>
  <read_first>components/UpdateGate.tsx</read_first>
  <action>
(EAS-06) On line 8 of `components/UpdateGate.tsx`, replace the string:
```
ios: 'https://apps.apple.com/app/dwella/id[APP_ID]',
```
with:
```
ios: 'https://apps.apple.com/app/dwella/id6760478576',
```

Change ONLY this one string. Do not modify any other code in the file.
  </action>
  <verify>
    <automated>grep -c "\[APP_ID\]" components/UpdateGate.tsx | grep "^0$" && grep -c "id6760478576" components/UpdateGate.tsx | grep "^1$" && echo "UpdateGate check passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '\[APP_ID\]' components/UpdateGate.tsx` returns no matches (exit code 1)
    - `grep 'id6760478576' components/UpdateGate.tsx` returns exactly 1 match
    - The full URL is `https://apps.apple.com/app/dwella/id6760478576`
  </acceptance_criteria>
  <done>UpdateGate.tsx contains the real App Store numeric ID (6760478576). No placeholder text remains.</done>
</task>

</tasks>

<verification>
Run the combined validation command from 18-VALIDATION.md:
```bash
node -e "const e=require('./eas.json'),a=require('./app.json'); console.assert(e.submit.production.ios.ascAppId==='6760478576'); console.assert(!!e.build.production.env.EXPO_PUBLIC_POSTHOG_API_KEY); console.assert(a.expo.ios.usesAppleSignIn===true); console.assert(a.expo.ios.appleTeamId==='35G5HXD9K9'); console.assert(a.expo.splash.backgroundColor==='#4F46E5'); console.assert(!!a.expo.ios.privacyManifests); console.log('All checks passed')"
```
and:
```bash
grep -c '\[APP_ID\]' components/UpdateGate.tsx
```
(must return 0)
</verification>

<success_criteria>
All seven requirement checks (EAS-01 through EAS-07) pass via the automated node assertion script. The grep for `[APP_ID]` returns 0 matches. The three files (app.json, eas.json, components/UpdateGate.tsx) are committed.
</success_criteria>

<output>
After completion, create `.planning/phases/18-apple-app-store-beta-testing-prep-configure-eas-build-app-json-metadata-bundle-id-provisioning-and-testflight-submission-readiness/18-01-SUMMARY.md`
</output>
