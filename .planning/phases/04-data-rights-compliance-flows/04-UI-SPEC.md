---
phase: 4
slug: data-rights-compliance-flows
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-06
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for the account deletion and data export UI additions to the existing profile screen. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (React Native — shadcn not applicable) |
| Preset | not applicable |
| Component library | React Native Paper (existing) |
| Icon library | @expo/vector-icons — MaterialCommunityIcons (existing) |
| Font | System default (existing — no custom font loaded) |

**Note:** This phase adds two new sections to the existing profile screen (`app/(tabs)/profile/index.tsx`). All UI must use the existing `GlassCard`, `GradientButton`, `useTheme()` hook, and React Native Paper components already in use on that screen. No new component library or design tokens are introduced.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Inline padding, icon-to-text gaps |
| sm | 8px | Compact element spacing, gap within rows |
| md | 16px | Default card padding, horizontal margins |
| lg | 24px | Section padding within gradient header |
| xl | 32px | Content bottom padding, major vertical gaps |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions: none — existing profile screen already uses this scale (16px card padding, 16px horizontal margins, 12px card gaps, 32px content bottom padding). New sections follow the same values.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Section Title | 11px | 700 (bold), uppercase, 0.5px letter-spacing | 1.2 |
| Heading | 20px | 700 (bold) | 1.2 |
| Button Label | 14px | 700 (bold) | 1.0 (single line) |

**Weights used:** 2 total — 400 (regular) for body text and descriptions, 700 (bold) for section titles, headings, and button labels.

**Source:** Extracted from existing profile screen `styles` object. All new text must match these exact values. The section title pattern (11px, uppercase, bold, secondary color) is mandatory for the "Your Data" and "Danger Zone" card headers.

---

## Color

| Role | Value (Light / Dark) | Usage |
|------|---------------------|-------|
| Dominant (60%) | `#F5F5F7` / `#1f1f1e` | Screen background (`colors.background`) |
| Secondary (30%) | `#FFFFFF` / `#161616` | GlassCard surfaces (`colors.surface`) |
| Accent (10%) | `#009688` / `#4DB6AC` | "Export My Data" GradientButton only (`colors.primary`) |
| Destructive | `#EF4444` / `#F87171` | "Delete Account" button text, Danger Zone border, destructive labels (`colors.error`) |

Accent reserved for: "Export My Data" `GradientButton` only. This matches the existing profile screen pattern where `GradientButton` is used for the primary CTA. All other elements on the new sections use outlined or text button styles with `colors.error` for destructive actions or `colors.textSecondary` for neutral labels.

Destructive reserved for: "Delete Account" button text color, "Delete Account" button border, Danger Zone section title text, the "DELETE" confirmation input border when focused.

---

## Component Inventory

All components below already exist in the project. No new components are created in this phase.

### Section 1: "Your Data" Card

| Element | Component | Props / Style |
|---------|-----------|---------------|
| Card wrapper | `GlassCard` | `style={[styles.sectionCard]}` — same as existing cards |
| Section title | `Text` (RN Paper) | "YOUR DATA", 11px, bold, uppercase, `colors.textSecondary` |
| Description | `Text` variant="bodySmall" | "Download a copy of all your data in JSON format.", 13px, `colors.textSecondary` |
| Export button | `GradientButton` | `icon="download"`, label "Export My Data" |
| Loading state | `GradientButton` | `loading={true}`, `disabled={true}`, label "Preparing Export..." |

**Placement:** Immediately after the "Help" GlassCard, before the current "Sign Out" button.

### Section 2: "Danger Zone" Card

| Element | Component | Props / Style |
|---------|-----------|---------------|
| Card wrapper | `GlassCard` | `style={[styles.sectionCard, { borderColor: colors.error, borderWidth: 1 }]}` |
| Section title | `Text` | "DANGER ZONE", 11px, bold, uppercase, `colors.error` |
| Warning text | `Text` variant="bodySmall" | See copywriting contract below, 13px, `colors.textSecondary` |
| Delete button | `Button` (RN Paper) | `mode="outlined"`, `icon="delete-outline"`, `textColor={colors.error}`, `style={{ borderColor: colors.error }}`, label "Delete Account" |

**Placement:** After the "Your Data" card. The current "Sign Out" button moves below this card (remains last element on screen).

### Section 3: Delete Confirmation Modal

| Element | Component | Props / Style |
|---------|-----------|---------------|
| Modal trigger | `Alert.alert` (React Native) | Title + message + two buttons (Cancel / Proceed to Confirm) |
| Confirmation screen | Inline view replacing profile content OR new modal view | See interaction flow below |
| Confirmation input | `TextInput` (RN Paper) | `mode="outlined"`, placeholder "Type DELETE to confirm", `outlineColor={colors.error}`, `activeOutlineColor={colors.error}` |
| Confirm button | `Button` (RN Paper) | `mode="contained"`, `buttonColor={colors.error}`, `textColor="#FFFFFF"`, label "Permanently Delete Account", `disabled` until input matches "DELETE" (case-insensitive) |
| Cancel button | `Button` (RN Paper) | `mode="text"`, label "Cancel" |
| Grace period notice | `Text` variant="bodySmall" | See copywriting contract, `colors.textSecondary` |

---

## Interaction Flows

### Export My Data Flow

```
1. User taps "Export My Data" button
2. Button enters loading state: label changes to "Preparing Export...", spinner appears, button disabled
3. Edge Function assembles JSON, uploads to Storage, returns signed URL
4. On success: system opens signed URL in device browser for download
   -> Toast: "Your data export is ready." (success)
5. On error: button returns to default state
   -> Toast: "{error message}" (error)
6. Button returns to default state after success or error
```

**Duration expectation:** Export may take 5-15 seconds depending on data volume. Loading state must persist for the full duration.

### Delete Account Flow

```
1. User taps "Delete Account" button
2. Alert.alert appears:
   Title: "Delete Your Account?"
   Message: "This will permanently delete all your data after a 30-day grace period. This action cannot be undone from the app."
   Buttons: [Cancel (cancel style)] [Proceed to Confirm (destructive style)]
3. If "Proceed to Confirm": navigate to confirmation view or show inline confirmation
4. User sees:
   - Grace period explanation text
   - TextInput with placeholder "Type DELETE to confirm"
   - "Permanently Delete Account" button (disabled, red background)
   - "Cancel" text button
5. User types "DELETE" (case-insensitive match)
   -> "Permanently Delete Account" button becomes enabled (opacity 1.0)
6. User taps "Permanently Delete Account"
   -> Button enters loading state
   -> Edge Function marks account for deletion
   -> Telegram notification sent (if linked)
   -> All sessions invalidated
   -> Local auth state cleared
   -> User is redirected to login screen
7. On error: button returns to default state
   -> Toast: "Could not delete account. Please try again." (error)
```

### State Transitions for Delete Button

| Input State | Button State | Button Opacity |
|-------------|-------------|----------------|
| Empty / partial text | Disabled | 0.5 |
| "DELETE" (any case) | Enabled | 1.0 |
| Processing | Loading + Disabled | 0.7 |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Export section title | YOUR DATA |
| Export description | Download a copy of all your data in JSON format. |
| Export button label | Export My Data |
| Export loading label | Preparing Export... |
| Export success toast | Your data export is ready. |
| Export error toast | Could not prepare your data export. Please try again. |
| Danger zone title | DANGER ZONE |
| Danger zone description | Deleting your account will permanently erase all your properties, tenants, payment records, and bot conversations after a 30-day grace period. Anonymized financial records are retained as required by law. |
| Delete button label | Delete Account |
| Delete alert title | Delete Your Account? |
| Delete alert message | This will permanently delete all your data after a 30-day grace period. This action cannot be undone from the app. |
| Delete alert cancel | Cancel |
| Delete alert continue | Proceed to Confirm |
| Confirmation heading | Confirm Account Deletion |
| Confirmation body | Your account will be scheduled for deletion. After 30 days, all personal data will be permanently erased. To cancel during the grace period, email the developer. |
| Confirmation input placeholder | Type DELETE to confirm |
| Confirmation button (disabled) | Permanently Delete Account |
| Confirmation button (loading) | Deleting... |
| Delete success (not shown — user is signed out) | N/A — user sees login screen |
| Delete error toast | Could not delete account. Please try again. |

---

## Accessibility

| Requirement | Implementation |
|-------------|----------------|
| Delete button contrast | White text on `colors.error` background meets WCAG AA (4.5:1+ on both light and dark themes) |
| Touch targets | All buttons use React Native Paper `Button` which enforces 48px minimum touch target height |
| Screen reader | Delete button: `accessibilityLabel="Delete your Dwella account"`, Export button: `accessibilityLabel="Export all your data as JSON"` |
| Confirmation input | `accessibilityHint="Type the word DELETE in capital letters to enable the delete button"` |
| Disabled state | Confirm button uses `disabled` prop (Paper handles reduced opacity and removes from tap targets) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | N/A | not applicable (React Native project) |

No third-party registries. No new dependencies. All UI built from existing React Native Paper components and project custom components (`GlassCard`, `GradientButton`).

---

## Screen Layout (Profile Screen — Updated)

Vertical scroll order after this phase:

```
1. DwellaHeader
2. Avatar gradient header (existing)
3. "Edit Profile" GlassCard (existing)
4. "Telegram Bot" GlassCard (existing)
5. "Security" GlassCard (existing)
6. "Appearance" GlassCard (existing)
7. "Help" GlassCard (existing)
8. "Your Data" GlassCard (NEW — Export My Data via GradientButton)
9. "Danger Zone" GlassCard (NEW — Delete Account, red border)
10. "Sign Out" outlined button (existing, moved below Danger Zone)
```

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
