---
phase: 4
slug: client-code-ux
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Phase 4: Client Code & UX.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Scope Note

Phase 4 is a hardening-only phase. No new screens, no new routes, no new visual components are introduced. The only user-visible change is a single error toast surfacing auth/profile sync failures (CLIENT-01). All other changes (subscription cleanup, env validation, push token audit) are invisible to the user at the UI layer.

This contract specifies the interaction and copy contract for that toast. All other design decisions inherit from the existing project design system without modification.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — React Native Paper (Snackbar) | Existing codebase |
| Preset | not applicable | — |
| Component library | react-native-paper (MD3) | Confirmed in `components/ToastProvider.tsx` |
| Icon library | @expo/vector-icons (MaterialCommunityIcons) | Existing codebase |
| Font | System default (San Francisco / Roboto) | Existing codebase — no custom font loaded |
| Theme system | `constants/theme.ts` LightTheme + DarkTheme | Existing — fully implemented |

---

## Spacing Scale

Pre-populated from `constants/spacing.ts`. No changes for this phase.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 12px | Default element spacing |
| lg | 16px | Section padding |
| xl | 20px | Layout gaps, screen padding |
| xxl | 24px | Section breaks |
| xxxl | 32px | Major layout gaps |

Exceptions:
- Touch targets (buttons, tab bar items): minimum 44px height per iOS HIG. Already enforced by existing GradientButton and CustomTabBar.
- Toast Snackbar: positioned at `bottom: 80` (above tab bar) — not a spacing token, it is a fixed layout constant in `components/ToastProvider.tsx`.

### Inherited Frozen Spacing Exception

`md` (12px) and `xl` (20px) are not multiples of 4 and would normally be blocked by the standard spacing scale rule. These values originate from the pre-existing `constants/spacing.ts`, which is shared across all screens and components in the project. Phase 4 is a hardening-only phase — modifying the spacing scale is explicitly out of scope and would introduce visual regressions across all screens. These values are declared as inherited frozen values: they exist in the codebase prior to this phase, they are not introduced by this phase, and they must not be changed in this phase. The standard 8-point scale exception is justified by the existing design system constraint.

---

## Typography

Pre-populated from `constants/typography.ts`. No new type roles introduced in this phase.

| Role | Size | Weight | Line Height | Source |
|------|------|--------|-------------|--------|
| Body | 15px | 500 | 1.5 (implicit) | `Typography.body` |
| Label / subtitle | 16px | 600 | 1.4 | `Typography.subtitle` |
| Heading | 22px | 700 | 1.2 | `Typography.heading2` |
| Display | 28px | 800 | 1.1 | `Typography.heading1` |

Toast message text uses `Typography.body` (15px, weight 500) via react-native-paper Snackbar defaults. No override needed.

### Inherited Frozen Typography Exception

Four weights (500, 600, 700, 800) are declared above. This exceeds the standard maximum of 2 weights. These weights originate from the pre-existing `constants/typography.ts`, which is shared across all screens and components. Phase 4 is a hardening-only phase — modifying the typography scale is explicitly out of scope and would introduce visual regressions across all screens. These weights are declared as inherited frozen values: they exist in the codebase prior to this phase, they are not introduced by this phase, and they must not be changed in this phase. The 2-weight maximum rule is overridden by the existing design system constraint.

---

## Color

Pre-populated from `constants/theme.ts`. No new colors introduced in this phase.

| Role | Light Value | Dark Value | Usage |
|------|-------------|------------|-------|
| Dominant (60%) | `#F5F5F7` | `#1f1f1e` | Screen backgrounds |
| Secondary (30%) | `#FFFFFF` | `#161616` | Cards, surfaces, nav |
| Accent (10%) | `#009688` / `#4DB6AC` | `#4DB6AC` | Primary brand elements only (see below) |
| Error / Destructive | `#EF4444` | `#F87171` | Error toasts, destructive actions |

Accent reserved for: primary CTA buttons (GradientButton), active tab indicator, primary action icons, header gradient. Not used on body text, labels, or informational UI.

### Error Toast Color Contract

The auth error toast (CLIENT-01) uses `colors.error` as its background:
- Light: `#EF4444`
- Dark: `#F87171`

This is already wired in `ToastProvider.tsx` via `BG['error']`. No new color token needed.

---

## Copywriting Contract

This section is the primary deliverable for Phase 4 UI work. All other sections are pre-populated from the existing design system.

| Element | Copy | Notes |
|---------|------|-------|
| Auth error toast message | "Profile sync failed. Some data may be outdated." | Non-blocking; user can still navigate |
| Auth error toast action | "Dismiss" | Dismiss only. No Retry button — profile sync is automatic on next auth state change. |
| Env validation throw (critical vars) | "[Dwella] Missing required environment variable: {KEY}\nAdd it to your .env file and restart the dev server." | Dev console only — not user-facing. Uses `requireEnv()` helper. |
| Env warning (optional vars) | "[Dwella] {KEY} not set — {feature} disabled" | Examples: "EXPO_PUBLIC_SENTRY_DSN not set — crash reporting disabled". Console warning only. |
| Push token missing projectId throw | "Expo projectId not found in app config" | Error thrown inside `registerPushToken()` — caught and logged, not surfaced to user |
| Push token DB write failure warning | "[Dwella] Failed to store push token: {error.message}" | `console.warn` only — best-effort registration, not user-facing |

### Toast Behavior Contract

| Property | Value |
|----------|-------|
| Display position | Bottom, 80px above tab bar (`bottom: 80`) |
| Auto-dismiss duration | 3000ms |
| Blocking | Non-blocking (`pointerEvents="box-none"`) |
| Retry action | None for auth errors — inform-only |
| Action label | "Dismiss" |

### Retry Decision (Claude's Discretion — Resolved)

Profile enrichment failures are network/DB transient issues. The auth listener in `_layout.tsx` automatically re-triggers enrichment on the next `onAuthStateChange` event or app resume. Adding a Retry button adds implementation complexity (re-triggering the enrichment IIFE imperatively) with minimal UX gain — the next navigation event or foreground resume typically resolves the failure. Decision: inform-only toast, no Retry action.

---

## Component Inventory

No new components are created in this phase. Components touched:

| Component / File | Change | UI Impact |
|-----------------|--------|-----------|
| `components/ToastProvider.tsx` | No change — reused as-is | None |
| `lib/toast.ts` | No change — reused as-is | None |
| `app/_layout.tsx` | Add `showToast(...)` + `Sentry.captureException()` to empty catch block | Error toast appears on auth sync failure |
| `constants/config.ts` | Replace `?? ''` with `requireEnv()` throw for critical vars | Dev-only: red error overlay on missing env vars |
| `lib/notifications.ts` | Add `projectId` param + DB write error handling | No user-visible change |

---

## States and Interactions

### New States Introduced

| State | Trigger | UI Representation |
|-------|---------|-------------------|
| Auth sync error | Profile upsert/select throws in `_layout.tsx` catch block | Error Snackbar at bottom of screen for 3 seconds |
| Startup crash (dev) | `EXPO_PUBLIC_SUPABASE_URL` or `EXPO_PUBLIC_SUPABASE_ANON_KEY` absent | React Native red error overlay (Metro dev server default — no custom UI needed) |

### No State Changes

All 10 subscription cleanup targets are already correct (RESEARCH.md audit table). No subscription-related UI state is modified.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| react-native-paper | Snackbar (existing) | not required — already in production |
| No third-party registries | — | not applicable |

No new packages are installed in this phase.

---

## Accessibility

Existing accessibility posture is preserved. The error toast (Snackbar from react-native-paper) already includes accessible text via the `message` prop. No additional ARIA labels or role props required.

Minimum touch target for the "Dismiss" action in the Snackbar: react-native-paper Snackbar action buttons meet the 44pt minimum by default.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
