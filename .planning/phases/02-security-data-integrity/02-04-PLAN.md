---
phase: 02-security-data-integrity
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: true
requirements: [SEC-01, SEC-02, DATA-02]
must_haves:
  truths:
    - "No Math.random() calls exist anywhere in lib/ or supabase/ directories"
    - "lib/bot.ts uses expo-crypto randomUUID() for token generation"
    - "lib/bot.ts uses secureRandomDigits() with Crypto.getRandomBytes() for verification codes"
    - "All payment status transitions in app code are valid according to the state machine"
    - "auto-confirm-payments only transitions paid->confirmed"
    - "mark-overdue only transitions pending->overdue"
  artifacts:
    - path: "lib/bot.ts"
      provides: "Verified: Crypto.randomUUID() for tokens, Crypto.getRandomBytes() for verification codes"
      contains: "Crypto.randomUUID"
    - path: "supabase/functions/auto-confirm-payments/index.ts"
      provides: "Verified: filters .eq('status', 'paid') before updating to confirmed"
      contains: "eq.*status.*paid"
    - path: "supabase/functions/mark-overdue/index.ts"
      provides: "Verified: filters .eq('status', 'pending') before updating to overdue"
      contains: "eq.*status.*pending"
  key_links:
    - from: "lib/bot.ts"
      to: "expo-crypto"
      via: "import * as Crypto from 'expo-crypto'"
      pattern: "Crypto\\.randomUUID|Crypto\\.getRandomBytes"
    - from: "supabase/functions/auto-confirm-payments/index.ts"
      to: "payments table"
      via: ".eq('status', 'paid') then update to confirmed"
      pattern: "eq.*status.*paid"
    - from: "supabase/functions/mark-overdue/index.ts"
      to: "payments table"
      via: ".eq('status', 'pending') then update to overdue"
      pattern: "eq.*status.*pending"
---

<objective>
Verify that crypto-secure token generation is complete and audit all payment state transitions against the DB trigger.

Purpose: Close SEC-01 (crypto UUID), SEC-02 (crypto verification codes), and DATA-02 (payment transition audit). Research confirmed SEC-01 and SEC-02 are already implemented — this plan verifies no regressions and documents the audit. DATA-02 requires verifying that every code path making a payment status change uses a transition the trigger allows.
Output: Verification results documented in summary. No code changes expected unless regressions are found.
</objective>

<execution_context>
@C:/Users/Spongeass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Spongeass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-data-integrity/02-CONTEXT.md
@.planning/phases/02-security-data-integrity/02-RESEARCH.md
@.planning/phases/02-security-data-integrity/02-01-SUMMARY.md

<interfaces>
<!-- Payment state machine from 02-01 trigger migration -->
Valid transitions:
- pending -> partial, paid, overdue
- partial -> paid, overdue
- overdue -> partial, paid
- paid -> confirmed
- confirmed -> paid (reversal)

<!-- Known crypto-secure implementations in lib/bot.ts -->
```typescript
import * as Crypto from 'expo-crypto';
const token = Crypto.randomUUID();  // SEC-01
function secureRandomDigits(length: number): string {  // SEC-02
  const bytes = Crypto.getRandomBytes(length);
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify crypto-secure token generation (SEC-01, SEC-02) and audit payment transitions (DATA-02)</name>
  <files></files>
  <read_first>
    - lib/bot.ts (verify Crypto.randomUUID and secureRandomDigits)
    - supabase/functions/auto-confirm-payments/index.ts (verify .eq('status', 'paid') filter)
    - supabase/functions/mark-overdue/index.ts (verify .eq('status', 'pending') filter)
    - supabase/functions/process-bot-message/index.ts (handleLogPayment, handleConfirmPayment — verify transitions)
    - hooks/usePayments.ts (any status update calls)
    - app/property/[id]/tenant/[tenantId]/payment/[paymentId].tsx (confirm payment UI)
    - app/property/[id]/tenant/[tenantId]/payment/mark-paid.tsx (mark paid UI)
    - lib/payments.ts (payment status helpers)
  </read_first>
  <action>
This is a verification-only task. Run the following checks and document results:

**SEC-01 + SEC-02: Crypto verification**

1. Run `grep -rn "Math.random" lib/ supabase/` — must return zero results.
2. Read `lib/bot.ts` and confirm:
   - `import * as Crypto from 'expo-crypto'` is present
   - `Crypto.randomUUID()` is used for token/UUID generation (not `Math.random()`)
   - `secureRandomDigits()` function uses `Crypto.getRandomBytes()` (not `Math.random()`)
3. Run `grep -rn "Math.random" app/ hooks/ components/` — confirm no security-sensitive usage anywhere else. `Math.random()` in non-security contexts (e.g., UI animation) is acceptable but should be noted.

**DATA-02: Payment transition audit**

Audit every code path that updates `payments.status` and verify it uses a valid transition:

4. `auto-confirm-payments/index.ts`:
   - Must filter `.eq('status', 'paid')` before updating to `confirmed`
   - Valid transition: paid -> confirmed
   - Document the exact filter and update lines

5. `mark-overdue/index.ts`:
   - Must filter `.eq('status', 'pending')` before updating to `overdue`
   - Valid transition: pending -> overdue
   - Check if it also handles `partial -> overdue` (also valid)
   - Document the exact filter and update lines

6. `process-bot-message/index.ts` `handleLogPayment`:
   - Transitions: pending -> paid/partial (line ~161-162, calculates newStatus)
   - Also handles overdue -> paid/partial (overdue payments can be marked paid)
   - Skips `confirmed` payments (line ~155-156)
   - All transitions valid: pending->paid, pending->partial, overdue->paid, overdue->partial
   - Document each transition

7. `process-bot-message/index.ts` `handleConfirmPayment`:
   - Checks `payment.status !== 'paid'` before confirming (line ~211)
   - Transition: paid -> confirmed
   - Valid

8. Payment detail screen (`app/property/[id]/tenant/[tenantId]/payment/[paymentId].tsx`):
   - Any confirm button transitions paid -> confirmed
   - Document the transition

9. Mark paid screen (`app/property/[id]/tenant/[tenantId]/payment/mark-paid.tsx`):
   - Transitions pending/partial/overdue -> paid
   - All valid

10. `hooks/usePayments.ts` — check for any direct status updates

If ANY code path uses an invalid transition (e.g., pending -> confirmed directly), fix it. Otherwise, document the audit results in the summary.

If `Math.random()` is found in any security path, replace it with `expo-crypto` equivalent. If found in non-security paths only, document as acceptable.
  </action>
  <verify>
    <automated>grep -rn "Math.random" lib/ supabase/ | grep -v node_modules || echo "PASS: No Math.random in lib/ or supabase/"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Math.random" lib/ supabase/` returns no results
    - lib/bot.ts contains `Crypto.randomUUID()` (not Math.random UUID)
    - lib/bot.ts contains `Crypto.getRandomBytes` in secureRandomDigits
    - auto-confirm-payments filters `.eq('status', 'paid')` before updating to confirmed
    - mark-overdue filters status before updating to overdue
    - handleLogPayment in process-bot-message skips confirmed payments and only transitions to paid/partial
    - handleConfirmPayment only transitions from paid to confirmed
    - No code path performs an invalid transition per the state machine
    - All audit findings documented in the plan summary
  </acceptance_criteria>
  <done>SEC-01 and SEC-02 confirmed complete with no regressions; DATA-02 audit confirms all payment transitions in app code are valid according to the DB trigger's allowed transitions</done>
</task>

</tasks>

<verification>
- `grep -rn "Math.random" lib/ supabase/` returns zero results
- Every payment status update code path documented with its transition
- All transitions match the allowed set in migration 017
</verification>

<success_criteria>
- Zero Math.random() usage in lib/ and supabase/ directories
- All payment transition code paths audited and documented as valid
- No code changes needed (verification-only) OR regressions fixed if found
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-data-integrity/02-04-SUMMARY.md`
</output>
