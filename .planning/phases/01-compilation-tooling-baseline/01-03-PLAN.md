---
phase: 01-compilation-tooling-baseline
plan: 03
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - eslint.config.js
  - hooks/useProperties.ts
  - hooks/useTenants.ts
  - hooks/usePayments.ts
  - hooks/useNotifications.ts
  - hooks/useExpenses.ts
  - hooks/useAllExpenses.ts
  - hooks/useBotConversations.ts
  - components/SocialAuthButtons.tsx
  - components/ChatBubble.tsx
  - components/Skeleton.tsx
  - components/CustomTabBar.tsx
  - components/EmptyState.tsx
  - components/GradientButton.tsx
  - components/PaymentStatusBadge.tsx
  - components/TourGuideCard.tsx
  - lib/analytics.ts
autonomous: true
gap_closure: true
requirements: [TS-03]

must_haves:
  truths:
    - "eslint-plugin-react-hooks is installed and configured so the inline disable comment in useProperties.ts does not produce a 'rule not found' error"
    - "All catch blocks in hooks/ and components/ use catch (err: unknown) not catch (err: any)"
    - "All MaterialCommunityIcons name props use a typed cast instead of as any"
    - "ChatBubble metadata, Skeleton width, and analytics Record use typed alternatives instead of as any"
  artifacts:
    - path: "eslint.config.js"
      provides: "ESLint config with react-hooks plugin registered"
      contains: "eslint-plugin-react-hooks"
    - path: "hooks/useProperties.ts"
      provides: "Typed error catch"
    - path: "components/CustomTabBar.tsx"
      provides: "Typed icon name prop"
  key_links:
    - from: "eslint.config.js"
      to: "hooks/useProperties.ts"
      via: "react-hooks/exhaustive-deps rule now recognized"
      pattern: "react-hooks"
---

<objective>
Fix ESLint configuration gap and remove all `as any` casts from hooks/ and components/ directories, plus lib/analytics.ts.

Purpose: Close Gap 1 (TS-03 partial) by eliminating `as any` violations in hooks, components, and lib files so that `npm run lint` moves toward zero errors. This plan handles the simpler, pattern-based fixes (error catches, icon names, metadata casts). Plan 04 handles app/ screens.

Output: Zero `no-explicit-any` errors in hooks/, components/, and lib/ directories; eslint-plugin-react-hooks configured.
</objective>

<execution_context>
@C:/Users/Spongeass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Spongeass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compilation-tooling-baseline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eslint-plugin-react-hooks and update ESLint config</name>
  <files>eslint.config.js, package.json</files>
  <read_first>
    - eslint.config.js (current ESLint config to modify)
    - package.json (current deps to check)
    - hooks/useProperties.ts (has the inline disable comment on line 57)
  </read_first>
  <action>
1. Install eslint-plugin-react-hooks:
   ```
   npm install --save-dev eslint-plugin-react-hooks
   ```

2. Update eslint.config.js to add the react-hooks plugin. Add this import at the top:
   ```javascript
   import reactHooks from 'eslint-plugin-react-hooks';
   ```

3. Add `'react-hooks': reactHooks` to the `plugins` object in the config.

4. Add `'react-hooks/exhaustive-deps': 'warn'` and `'react-hooks/rules-of-hooks': 'error'` to the `rules` object.

This ensures the inline `// eslint-disable-line react-hooks/exhaustive-deps` comment in hooks/useProperties.ts line 57 is recognized by ESLint instead of producing a "Definition for rule not found" error.

Do NOT change the severity of any existing rules. Do NOT modify any other config structure.
  </action>
  <verify>
    <automated>cd "C:/Users/Spongeass/Desktop/Claude Code/Dwella v2" && npm run lint 2>&1 | grep -c "Definition for rule"</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.js contains `import reactHooks from 'eslint-plugin-react-hooks'`
    - eslint.config.js contains `'react-hooks': reactHooks` in plugins
    - eslint.config.js contains `'react-hooks/rules-of-hooks': 'error'`
    - eslint.config.js contains `'react-hooks/exhaustive-deps': 'warn'`
    - package.json devDependencies contains `eslint-plugin-react-hooks`
    - `npm run lint 2>&1 | grep "Definition for rule"` returns 0 matches
  </acceptance_criteria>
  <done>eslint-plugin-react-hooks installed and configured; no more "rule not found" errors from inline disable comments</done>
</task>

<task type="auto">
  <name>Task 2: Remove all as any casts from hooks/, components/, and lib/analytics.ts</name>
  <files>
    hooks/useProperties.ts, hooks/useTenants.ts, hooks/usePayments.ts,
    hooks/useNotifications.ts, hooks/useExpenses.ts, hooks/useAllExpenses.ts,
    hooks/useBotConversations.ts,
    components/SocialAuthButtons.tsx, components/ChatBubble.tsx,
    components/Skeleton.tsx, components/CustomTabBar.tsx,
    components/EmptyState.tsx, components/GradientButton.tsx,
    components/PaymentStatusBadge.tsx, components/TourGuideCard.tsx,
    lib/analytics.ts
  </files>
  <read_first>
    - hooks/useProperties.ts (catch err: any on line 52)
    - hooks/useTenants.ts (catch err: any on line 35)
    - hooks/usePayments.ts (catch err: any on line 36)
    - hooks/useNotifications.ts (catch err: any on line 29)
    - hooks/useExpenses.ts (catch err: any on line 35)
    - hooks/useAllExpenses.ts (catch err: any on line 37)
    - hooks/useBotConversations.ts (catch err: any on line 26)
    - components/SocialAuthButtons.tsx (catch err: any on lines 53, 72)
    - components/ChatBubble.tsx (message.metadata as any on line 19)
    - components/Skeleton.tsx (width as any on line 39)
    - components/CustomTabBar.tsx (icon name as any on line 47)
    - components/EmptyState.tsx (icon name as any on line 20)
    - components/GradientButton.tsx (icon name as any on lines 40, 69)
    - components/PaymentStatusBadge.tsx (icon name as any on line 38)
    - components/TourGuideCard.tsx (icon as any line 82, router.replace as any line 55)
    - lib/analytics.ts (Record string any on line 52)
    - lib/types.ts (for BotConversation metadata type reference)
  </read_first>
  <action>
Apply these mechanical fixes by pattern:

**Pattern A: catch (err: any) -> catch (err: unknown)**
In ALL 9 catch blocks across the 7 hooks + SocialAuthButtons.tsx + app/payments/index.tsx:
- Change `catch (err: any)` to `catch (err: unknown)`
- Change `err.message` to `err instanceof Error ? err.message : 'Unknown error'`
- Files: hooks/useProperties.ts:52, hooks/useTenants.ts:35, hooks/usePayments.ts:36, hooks/useNotifications.ts:29, hooks/useExpenses.ts:35, hooks/useAllExpenses.ts:37, hooks/useBotConversations.ts:26, components/SocialAuthButtons.tsx:53 and :72

**Pattern B: MaterialCommunityIcons name={x as any} -> typed cast**
Add this import to each file that uses the icon cast (if not already present):
```typescript
import type { ComponentProps } from 'react';
```
Then replace `name={iconVar as any}` with `name={iconVar as ComponentProps<typeof MaterialCommunityIcons>['name']}`.
- Files and lines: components/CustomTabBar.tsx:47, components/EmptyState.tsx:20, components/GradientButton.tsx:40 and :69, components/PaymentStatusBadge.tsx:38, components/TourGuideCard.tsx:82

**Pattern C: Router cast -> as Href (established pattern from _layout.tsx)**
In components/TourGuideCard.tsx:55, change `router.replace(TOUR_STEPS[next].route as any)` to `router.replace(TOUR_STEPS[next].route as Href)`. Add `import { type Href } from 'expo-router'` if not present (router is already imported from expo-router).

**Pattern D: Specific one-off fixes**
- components/ChatBubble.tsx:19 — Change `(message.metadata as any)?.pending_action` to `(message.metadata as Record<string, unknown>)?.pending_action`. The metadata field is JSONB from Supabase, typed as `Json | null` in lib/types.ts.
- components/Skeleton.tsx:39 — Change `width: width as any` to `width: width as number | `${number}%``. The width prop is `number | string` but StyleSheet expects specific types. Use: `width: width as number`  (the component only receives number values from callers).
- lib/analytics.ts:52 — Change `Record<string, any>` to `Record<string, unknown>`.

After all changes, run `npx tsc --noEmit` to confirm TypeScript still compiles clean. If any type error arises from the narrower casts, adjust the cast to the most specific type that satisfies both the runtime value and the type checker.
  </action>
  <verify>
    <automated>cd "C:/Users/Spongeass/Desktop/Claude Code/Dwella v2" && npx tsc --noEmit && grep -rn "as any\|: any" hooks/ components/ lib/analytics.ts | grep -v node_modules | grep -v ".d.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "as any" hooks/` returns zero matches
    - `grep -rn ": any" hooks/` returns zero matches (no catch (err: any) remains)
    - `grep -rn "as any" components/` returns zero matches
    - `grep -n "as any" lib/analytics.ts` returns zero matches
    - `grep -n "Record<string, any>" lib/analytics.ts` returns zero matches
    - `npx tsc --noEmit` exits 0
    - `npm run lint 2>&1 | grep "no-explicit-any" | grep -c "hooks/\|components/\|lib/analytics"` returns 0
  </acceptance_criteria>
  <done>Zero as any casts remain in hooks/, components/, and lib/analytics.ts; TypeScript still compiles clean</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` exits 0 (compilation baseline preserved)
2. `npm run lint 2>&1 | grep "Definition for rule"` returns 0 matches (react-hooks configured)
3. `grep -rn "as any\|: any" hooks/ components/ lib/analytics.ts | grep -v node_modules` returns 0 matches
4. `npm run lint 2>&1 | grep "no-explicit-any"` count is reduced (remaining errors only in app/ files, handled by Plan 04)
</verification>

<success_criteria>
- eslint-plugin-react-hooks installed and configured in eslint.config.js
- All `as any` casts in hooks/, components/, and lib/analytics.ts replaced with typed alternatives
- TypeScript compilation still exits 0
- ESLint no-explicit-any errors from hooks/ and components/ directories are eliminated
</success_criteria>

<output>
After completion, create `.planning/phases/01-compilation-tooling-baseline/01-03-SUMMARY.md`
</output>
