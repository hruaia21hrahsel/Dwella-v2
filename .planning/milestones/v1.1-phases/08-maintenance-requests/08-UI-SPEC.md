---
phase: 8
slug: maintenance-requests
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-21
---

# Phase 8 — UI Design Contract: Maintenance Requests

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (React Native — shadcn not applicable) | Project architecture |
| Preset | not applicable | — |
| Component library | react-native-paper | RESEARCH.md Standard Stack |
| Icon library | @expo/vector-icons MaterialCommunityIcons | RESEARCH.md Standard Stack |
| Font | System default (Expo managed workflow — SF Pro on iOS, Roboto on Android) | Project default |
| Theme | `useTheme()` from `@/lib/theme-context` — light + dark token map | constants/theme.ts |

---

## Spacing Scale

All values are multiples of 4. Source: measured from existing components (DocumentCard, EmptyState, CategoryFilterBar).

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gap, chip vertical padding, dot separators |
| sm | 8px | Card `marginBottom`, icon container internal gap, chip `marginRight` 6px rounds to this tier |
| md | 16px | Screen horizontal padding, card `paddingHorizontal`, filter bar left offset |
| lg | 24px | Section separators, form field vertical gap |
| xl | 32px | Empty state container padding, screen top padding |
| 2xl | 48px | Large section breaks (e.g. submit form bottom safe area) |
| 3xl | 64px | FAB clearance from bottom of scroll list |

Exceptions:
- Touch targets for icon-only buttons: minimum 44×44px (delete, close, camera buttons) — enforced via `hitSlop={8}` on 36px icon or explicit `width: 44, height: 44` — pattern from DocumentCard `deleteBtn`
- Status chip padding: `paddingHorizontal: 12, paddingVertical: 4` (nearest multiple of 4; matches CategoryFilterBar chip rhythm)
- Activity timeline connector line: 2px wide, left-aligned at 20px from card left edge

---

## Typography

Source: Measured from DocumentCard, EmptyState, CategoryFilterBar. Weights extracted from StyleSheet declarations.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 | 1.5 (24px) | Request title in card, description in detail screen, notes in timeline |
| Label | 14px | 400 | 1.43 (20px) | Meta text (timestamp, property name, status log note), filter chip labels |
| Heading | 18px | 700 | 1.22 (22px) | Screen section headings, empty state title, detail screen request title |
| Display | 13px | 400 | 1.38 (18px) | Filter chips (CategoryFilterBar pattern), priority badge labels |

No additional weights. The codebase uses only 400 (regular) and 700 (bold) — confirmed in DocumentCard (`fontWeight: '400'`) and EmptyState (`fontWeight: '700'`).

Note: Heading was raised from 17px to 18px to ensure perceptible visual hierarchy above the 16px Body size (minimum 2px separation required).

---

## Color

Source: constants/theme.ts LightTheme. All values from the established theme token map. New maintenance status colors defined in CONTEXT.md code_context and RESEARCH.md STATUS_COLORS.

### Surface Colors (60/30/10)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `colors.background` = `#F5F5F7` | Screen backgrounds, list scroll area |
| Secondary (30%) | `colors.surface` = `#FFFFFF` | Request cards, detail screen sections, form inputs, timeline entries |
| Accent (10%) | `colors.primary` = `#009688` | Active filter chips, primary action buttons (Acknowledge Request / Start Work / Mark Resolved / Close Request), FAB, Submit Request button |
| Destructive | `colors.error` = `#EF4444` | Archive/delete request confirmation dialogs only |

Accent reserved for: active filter chip fill, the single primary action button on the detail screen per status step, the FAB on the list screen, and the Submit Request form button. Not used for status chips or priority indicators (those use their own semantic colors below).

### Maintenance Status Colors

Defined in `lib/maintenance.ts` STATUS_COLORS. These are semantic overrides, not accent usage.

| Status | Color | Soft Background |
|--------|-------|-----------------|
| open | `#94A3B8` (gray) | `rgba(148,163,184,0.12)` |
| acknowledged | `#3B82F6` (blue) | `rgba(59,130,246,0.12)` |
| in_progress | `#F59E0B` (amber) | `rgba(245,158,11,0.12)` |
| resolved | `#10B981` (green) | `rgba(16,185,129,0.12)` |
| closed | `#0D9488` (teal) | `rgba(13,148,136,0.12)` |

### Priority Colors

| Priority | Color | Soft Background |
|----------|-------|-----------------|
| low | `#94A3B8` (gray) | `rgba(148,163,184,0.12)` |
| normal | `#3B82F6` (blue) | `rgba(59,130,246,0.12)` |
| urgent | `#EF4444` (red) | `rgba(239,68,68,0.12)` |

---

## Component Inventory

All components either exist and are reused directly, or are new thin wrappers following existing patterns.

| Component | Status | Pattern Source |
|-----------|--------|---------------|
| `MaintenanceRequestCard` | New | Mirrors DocumentCard layout: 72px row height, 40×40 icon container, primarySoft bg, 16px paddingHorizontal, 8px marginBottom, borderRadius 12, border 1px |
| `MaintenancePhotoUploader` | New | Adapted from DocumentUploader — adds camera button alongside gallery button; two TouchableOpacity side by side; 5-photo limit enforced with disabled state |
| `MaintenanceFilterBar` | New | Adapted from CategoryFilterBar — accepts generic string enum instead of DocumentCategory; two rows of chips (status row + priority row) separated by 8px gap |
| `MaintenanceTimeline` | New | Vertical list of log entries; each entry: 36px circular icon container at left, connector line (2px, `colors.divider`) between entries, timestamp at 13px/400, note text at 14px/400, actor name at 14px/700 |
| `CategoryFilterBar` | Reuse as-is | Status filter chips in single horizontal scroll row |
| `EmptyState` | Reuse as-is | icon="wrench-outline" for all empty maintenance lists |
| `DocumentViewer` | Reuse as-is | Full-screen photo viewer for request photos |
| `ConfirmDialog` | Reuse as-is | Archive/close request destructive confirmation |
| `ListSkeleton` | Reuse as-is | Loading state for request lists |
| `GlassCard` | Reuse as-is | Detail screen section containers (photos section, cost log section) |
| `DwellaHeader` | Reuse as-is | All new screens |

---

## Screen Interaction Contracts

### 1. Request List Screen (`app/maintenance/index.tsx` + `app/property/[id]/maintenance.tsx`)

Focal point: the grouped status list. The filter bar anchors the top; visual weight lands on the first open-status group of cards. Users scan vertically from highest-priority open cards downward.

- Header: DwellaHeader with screen title "Maintenance" (standalone) or "Maintenance" (contextual)
- Filter bar: `MaintenanceFilterBar` with two rows — row 1: status chips (All | Open | Acknowledged | In Progress | Resolved | Closed), row 2: priority chips (All | Low | Normal | Urgent). Sort toggle button at far right of row 1: icon `sort-variant` cycling Newest first / Oldest first. `accessibilityLabel` for sort toggle: `"Sort requests, currently {sortOrder}"` (e.g. "Sort requests, currently Newest first").
- List: grouped by status sections — section headers at 14px/700 `colors.textSecondary`, uppercase. Section order: Open, Acknowledged, In Progress, Resolved, Closed
- Each card: tap opens `app/maintenance/[id].tsx`. Long-press: no action (no context menu)
- FAB: bottom-right, `wrench-plus-outline` icon, label "New Request" (tenant only — hide for landlord-only users). Navigates to `app/maintenance/submit.tsx`
- Loading: `ListSkeleton` during initial fetch
- Empty (no requests at all): EmptyState icon="wrench-outline" title="No maintenance requests" subtitle="Tap the button below to submit your first request." actionLabel="Submit Request" (tenant view) or subtitle="No requests have been submitted yet." with no action button (landlord view)
- Empty (filtered, no results): EmptyState icon="filter-outline" title="No matching requests" subtitle="Try adjusting your filters." no action button

### 2. Submit Request Screen (`app/maintenance/submit.tsx`)

- Full-screen modal pushed via router. Back button dismisses.
- Form fields in order:
  1. Title (required): single-line TextInput, label "Title", placeholder "e.g. Leaking kitchen tap"
  2. Description (required): multi-line TextInput, label "Description", minHeight 100px, 4 lines visible, placeholder "Describe the issue in detail"
  3. Priority (required, default Normal): horizontal segmented control — three TouchableOpacity cells in a row, equal width, 48px height, borderRadius 10, border `colors.border`. Active cell: `colors.primary` fill, `colors.textOnPrimary` label. Inactive: `colors.surface` fill, `colors.textSecondary` label. Labels: "Low" | "Normal" | "Urgent"
  4. Photos (optional, max 5): `MaintenancePhotoUploader` — two buttons side by side: "Take Photo" (camera-outline icon) and "Choose from Library" (image-outline icon). Thumbnails appear below buttons in a horizontal scroll row, each 80×80px, borderRadius 8. Each thumbnail has an "×" close button (top-right, 20×20px, `colors.error` icon) to remove. `accessibilityLabel` for each remove button: `"Remove photo {n}"` (e.g. "Remove photo 1", "Remove photo 2").
  5. Upload progress: ActivityIndicator (react-native-paper) while photos upload post-insert. Not a progress bar — spinner only (Claude's discretion resolved to spinner: simpler, no byte count available from Supabase Storage)
- Primary CTA: GradientButton "Submit Request" at bottom, full width, disabled until title + description filled
- Error feedback: toast via `useToastStore` (not inline form errors)

### 3. Request Detail Screen (`app/maintenance/[id].tsx`)

Focal point: the primary action button (landlord) or the status chip + timeline (tenant). Landlord eye path: status chip at top of info card → timeline → action button at bottom. Tenant eye path: status chip → timeline. GlassCard sections are stacked vertically with 16px gaps; the action area uses `colors.primary` to draw landlord attention to the next required step.

Visible to both tenant and landlord. Role determines which elements are active.

- Header: DwellaHeader, title = request title (truncated to 1 line)
- Section 1 — Request Info: GlassCard containing description (16px/400), priority chip, submitted-by name, submitted-at relative timestamp
- Section 2 — Photos: horizontal ScrollView of photo thumbnails (80×80px, borderRadius 8). Tap any thumbnail opens `DocumentViewer` in full-screen modal. Show only if `photo_paths.length > 0`
- Section 3 — Activity Timeline: `MaintenanceTimeline`, all status log entries, newest at bottom. Each entry shows: status icon (circle icon colored per STATUS_COLORS), actor name (14px/700), "changed status to X" text (14px/400), timestamp (13px/400 `colors.textSecondary`), optional note text below (14px/400 `colors.textSecondary`, italic)
- Section 4 — Landlord Action Area (landlord only, hidden for tenants):
  - If status is not "closed": primary action button (GradientButton, full width) with label from NEXT_STATUS_LABEL map
  - Optional notes TextInput below: label "Add a note (optional)", single-line, placeholder "e.g. Plumber scheduled for Thursday"
  - If advancing to "resolved": cost field appears below notes with label "Log repair cost?", TextInput with keyboardType="decimal-pad", prefix "£" label at left, placeholder "0.00". Not required — blank = no expense created.
  - Submitting action: show ActivityIndicator in button while awaiting DB write + push notification

### 4. Priority Segmented Control

- Three equal-width segments in a `flexDirection: 'row'` container
- Container: borderRadius 10, borderWidth 1, borderColor `colors.border`, overflow hidden
- Each segment: flex 1, height 48, centered text, no gap between segments (adjacents share border)
- Active segment: backgroundColor `colors.primary`, text `colors.textOnPrimary` at 14px/700
- Inactive segment: backgroundColor `colors.surface`, text `colors.textSecondary` at 14px/400

### 5. Status Chip (on request cards)

- Pill shape: borderRadius 12, paddingHorizontal 12, paddingVertical 4
- backgroundColor: STATUS_COLORS soft background (`rgba(...)`)
- Text: STATUS_LABELS[status] at 13px/400, color: STATUS_COLORS[status]

### 6. Priority Indicator (on request cards)

- Small colored dot (8×8px, borderRadius 4) + text label inline
- Dot color: PRIORITY_COLORS[priority] (urgent=`#EF4444`, normal=`#3B82F6`, low=`#94A3B8`)
- Label: priority name at 13px/400 `colors.textSecondary`

---

## Copywriting Contract

| Element | Copy | Notes |
|---------|------|-------|
| Primary CTA (tenant submit) | "Submit Request" | GradientButton on submit form |
| Primary CTA (landlord acknowledge) | "Acknowledge Request" | Detail screen action button when status=open |
| Primary CTA (landlord start work) | "Start Work" | Detail screen action button when status=acknowledged |
| Primary CTA (landlord resolve) | "Mark Resolved" | Detail screen action button when status=in_progress |
| Primary CTA (landlord close) | "Close Request" | Detail screen action button when status=resolved |
| Empty state — tenant, no requests | Heading: "No maintenance requests" / Body: "Submit a request to let your landlord know about any issues in your home." / Button: "Submit Request" | |
| Empty state — landlord, no requests | Heading: "No maintenance requests" / Body: "You'll see requests here once your tenants submit them." / No action button | |
| Empty state — filtered, no results | Heading: "No matching requests" / Body: "Try adjusting your filters." / No action button | |
| Error — submit failed | "Could not submit your request. Please check your connection and try again." | Toast, error variant |
| Error — photo upload failed | "Request submitted, but one or more photos failed to upload. You can resubmit with photos." | Toast, warning variant |
| Error — status update failed | "Could not update the request status. Please try again." | Toast, error variant |
| Error — invalid status transition (DB trigger) | "This status change is not allowed." | Toast, error variant — surfaces the DB trigger exception |
| Destructive — close request confirmation | Title: "Close this request?" / Body: "Closing marks this request as resolved and complete. This cannot be undone." / Confirm: "Close Request" / Cancel: "Cancel" | ConfirmDialog component |
| Push notification — new request (to landlord) | Title: "New Maintenance Request" / Body: "[Tenant name]: [description first 80 chars]" | send-push payload |
| Push notification — status changed (to tenant) | Title: "Request Update" / Body: "Your maintenance request is now [STATUS_LABELS[status]]." | send-push payload |
| Tools menu card label | "Maintenance" | tools/index.tsx |
| Tools menu card description | "Track and manage maintenance requests" | tools/index.tsx |
| Cost log section label | "Log repair cost?" | Detail screen, appears when advancing to resolved |
| Notes field placeholder | "Add a note (optional)" | Detail screen, all status transitions |

---

## Registry Safety

This is a React Native / Expo project. shadcn is not initialized and not applicable.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| N/A — React Native project | N/A | not applicable |

No third-party component registries. All components are hand-rolled following existing project patterns, using react-native-paper primitives and MaterialCommunityIcons.

---

## Icons Reference

All icons from MaterialCommunityIcons. Sizes follow existing component conventions (22px in list rows, 40px in EmptyState, 20–24px in action buttons).

| Context | Icon Name | Size | Color |
|---------|-----------|------|-------|
| Tools menu card | `wrench-outline` | 24 | `#14B8A6` (teal, matches existing card color scheme) |
| FAB | `wrench-plus-outline` | 24 | `colors.textOnPrimary` |
| Empty state (maintenance) | `wrench-outline` | 40 | `colors.primary` |
| Status: open | `clock-outline` | 20 | `#94A3B8` |
| Status: acknowledged | `eye-outline` | 20 | `#3B82F6` |
| Status: in_progress | `hammer-wrench` | 20 | `#F59E0B` |
| Status: resolved | `check-circle-outline` | 20 | `#10B981` |
| Status: closed | `archive-outline` | 20 | `#0D9488` |
| Priority: urgent | `alert-circle-outline` | 16 | `#EF4444` |
| Priority: normal | `minus-circle-outline` | 16 | `#3B82F6` |
| Priority: low | `chevron-down-circle-outline` | 16 | `#94A3B8` |
| Photo: take camera | `camera-outline` | 22 | `colors.primary` |
| Photo: gallery | `image-outline` | 22 | `colors.primary` |
| Photo: remove | `close-circle` | 20 | `colors.error` — `accessibilityLabel`: "Remove photo {n}" |
| Sort toggle | `sort-variant` | 22 | `colors.textSecondary` — `accessibilityLabel`: "Sort requests, currently {sortOrder}" |
| Timeline connector | line (View, not icon) | 2px wide | `colors.divider` |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
