---
phase: 01-compilation-tooling-baseline
plan: 04
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - app/tools/ai-search.tsx
  - app/tools/_layout.tsx
  - app/log-payment.tsx
  - app/payments/index.tsx
  - app/reminders/index.tsx
  - app/expenses/index.tsx
  - app/onboarding/index.tsx
  - app/notifications/index.tsx
  - app/(auth)/phone-verify.tsx
  - app/(tabs)/dashboard/index.tsx
  - app/(tabs)/profile/index.tsx
  - app/(tabs)/properties/[id].tsx
  - app/(tabs)/tools/index.tsx
  - app/property/[id]/expenses/add.tsx
  - app/property/[id]/expenses/index.tsx
  - app/property/[id]/expenses/[expenseId].tsx
  - app/property/[id]/tenant/[tenantId]/index.tsx
  - app/property/[id]/tenant/[tenantId]/payment/mark-paid.tsx
  - hooks/useDashboard.ts
autonomous: false
gap_closure: true
requirements: [TS-03, EDGE-04]

must_haves:
  truths:
    - "npm run lint exits with zero no-explicit-any errors across the entire codebase"
    - "npm run lint produces zero errors total (only warnings remain)"
    - "npx tsc --noEmit still exits 0 after all changes"
    - "Sentry DSN setup steps are documented for user action"
  artifacts:
    - path: "app/tools/ai-search.tsx"
      provides: "Typed search result interfaces replacing all (item as any) casts"
      contains: "interface"
    - path: "app/(tabs)/dashboard/index.tsx"
      provides: "Typed icon name prop"
    - path: "hooks/useDashboard.ts"
      provides: "Typed property/tenant/payment data without as any[]"
  key_links:
    - from: "app/tools/ai-search.tsx"
      to: "lib/types.ts"
      via: "search result interfaces matching DB schema"
      pattern: "interface.*SearchResult"
---

<objective>
Remove all remaining `as any` casts from app/ screens and hooks/useDashboard.ts so that `npm run lint` exits with zero errors. Includes Sentry DSN human checkpoint.

Purpose: Complete Gap 1 closure (TS-03) by eliminating all `no-explicit-any` errors across the codebase. After this plan, `npm run lint` can serve as a clean CI gate. Also documents Gap 2 (EDGE-04) Sentry DSN as a human checkpoint.

Output: Zero `no-explicit-any` ESLint errors; lint exits with only warnings (no errors); Sentry production setup documented.
</objective>

<execution_context>
@C:/Users/Spongeass/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Spongeass/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compilation-tooling-baseline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type ai-search.tsx search results and fix useDashboard/log-payment/payments data casts</name>
  <files>
    app/tools/ai-search.tsx, hooks/useDashboard.ts, app/log-payment.tsx,
    app/payments/index.tsx, app/reminders/index.tsx, app/expenses/index.tsx,
    app/(tabs)/dashboard/index.tsx, app/property/[id]/expenses/index.tsx
  </files>
  <read_first>
    - app/tools/ai-search.tsx (13 as any casts for untyped search results — lines 78-142)
    - hooks/useDashboard.ts (as any[] on lines 103 and 111)
    - app/log-payment.tsx (as any[] and : any on line 94)
    - app/payments/index.tsx (p: any on line 138, catch err: any on line 147, icon as any on line 431)
    - app/reminders/index.tsx (item: any on line 86)
    - app/expenses/index.tsx (p: any on line 67)
    - app/(tabs)/dashboard/index.tsx (icon as any on line 78, e: any on line 156)
    - app/property/[id]/expenses/index.tsx (p: any on line 46)
    - lib/types.ts (for Payment, Tenant, Property interface shapes)
  </read_first>
  <action>
**Fix 1: app/tools/ai-search.tsx — Define search result interfaces**

Add these interfaces near the top of the file (after imports):

```typescript
interface PaymentSearchResult {
  id: string;
  tenant_id: string;
  property_id: string;
  tenant_name: string;
  flat_no: string;
  property_name: string;
  status: string;
  month: number;
  year: number;
  amount_paid: number;
  amount_due: number;
}

interface TenantSearchResult {
  id: string;
  tenant_name: string;
  flat_no: string;
  property_name: string;
  monthly_rent: number;
  due_day: number;
  property_id: string;
}

interface PropertySearchResult {
  id: string;
  name: string;
  address: string;
  city: string;
  total_units: number;
}
```

Then replace all `(item as any).field` patterns:
- In the payments renderItem callback (~line 76-100): type `item` as `PaymentSearchResult` using `const result = item as PaymentSearchResult;` at the top of the callback, then use `result.tenant_id`, `result.property_id`, etc.
- In the tenants renderItem callback (~line 112-124): type `item` as `TenantSearchResult`
- In the properties renderItem callback (~line 135-143): type `item` as `PropertySearchResult`

**Fix 2: hooks/useDashboard.ts — Type the property/payment data**

Read the file to understand the data shape. The `payResult.data` comes from a Supabase query. Define a local interface for the payment query result shape:

```typescript
interface DashboardPayment {
  tenant_id: string;
  status: string;
  amount_paid: number;
  amount_due: number;
}
```

Replace line 103 `(payResult.data ?? []) as any[]` with `(payResult.data ?? []) as DashboardPayment[]`.

For line 111 `(property.tenants as any[])`, define:

```typescript
interface DashboardTenant {
  id: string;
  is_archived: boolean;
  tenant_name: string;
  due_day: number;
}
```

Replace with `(property.tenants as DashboardTenant[])`.

**Fix 3: app/log-payment.tsx line 94**
Change `((p.tenants as any[]) ?? []).filter((t: any) => !t.is_archived)` to:
`((p.tenants as Array<{ is_archived: boolean }>) ?? []).filter((t) => !t.is_archived)`

**Fix 4: app/payments/index.tsx**
- Line 138: Change `(p: any)` to a typed mapper. Read the file to see the query shape and define an inline type or interface for the payment data.
- Line 147: Change `catch (err: any)` to `catch (err: unknown)` and `err.message` to `err instanceof Error ? err.message : 'Unknown error'`
- Line 431: Change `name={opt.icon as any}` to `name={opt.icon as ComponentProps<typeof MaterialCommunityIcons>['name']}` (add `import type { ComponentProps } from 'react'`)

**Fix 5: app/reminders/index.tsx line 86**
Change `(item: any)` to a typed parameter. Read the file to determine the item shape from the query, then type accordingly (likely needs a `ReminderItem` interface with `status`, `due_day`, etc.)

**Fix 6: app/expenses/index.tsx line 67 and app/property/[id]/expenses/index.tsx line 46**
Change `(p: any) => s + (p.amount_paid ?? 0)` to `(p: { amount_paid?: number }) => s + (p.amount_paid ?? 0)` (inline typed parameter).

**Fix 7: app/(tabs)/dashboard/index.tsx**
- Line 78: Change `name={STAT_ICONS[label] as any}` to `name={STAT_ICONS[label] as ComponentProps<typeof MaterialCommunityIcons>['name']}` (add `import type { ComponentProps } from 'react'`)
- Line 156: Change `(e: any) => s + (e.amount ?? 0)` to `(e: { amount?: number }) => s + (e.amount ?? 0)`

After all changes, run `npx tsc --noEmit` to confirm zero TypeScript errors.
  </action>
  <verify>
    <automated>cd "C:/Users/Spongeass/Desktop/Claude Code/Dwella v2" && npx tsc --noEmit && grep -rn "as any\|: any" app/tools/ai-search.tsx hooks/useDashboard.ts app/log-payment.tsx app/payments/index.tsx app/reminders/index.tsx app/expenses/index.tsx "app/(tabs)/dashboard/index.tsx" "app/property/[id]/expenses/index.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as any" app/tools/ai-search.tsx` returns 0
    - `grep -c "as any" hooks/useDashboard.ts` returns 0
    - `grep -c "as any" app/log-payment.tsx` returns 0
    - `grep -c ": any" app/payments/index.tsx` returns 0
    - `grep -c ": any" app/reminders/index.tsx` returns 0
    - app/tools/ai-search.tsx contains `interface PaymentSearchResult`
    - app/tools/ai-search.tsx contains `interface TenantSearchResult`
    - app/tools/ai-search.tsx contains `interface PropertySearchResult`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All as any casts in ai-search, useDashboard, log-payment, payments, reminders, expenses, and dashboard are replaced with typed alternatives</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining app/ screen as any casts (header styles, router, profile, phone-verify, tenant detail)</name>
  <files>
    app/tools/_layout.tsx, app/onboarding/index.tsx,
    app/notifications/index.tsx, app/(auth)/phone-verify.tsx,
    app/(tabs)/profile/index.tsx, app/(tabs)/properties/[id].tsx,
    app/(tabs)/tools/index.tsx,
    app/property/[id]/expenses/add.tsx,
    app/property/[id]/expenses/[expenseId].tsx,
    app/property/[id]/tenant/[tenantId]/index.tsx,
    app/property/[id]/tenant/[tenantId]/payment/mark-paid.tsx
  </files>
  <read_first>
    - app/tools/_layout.tsx (headerStyle as any on line 11)
    - app/notifications/index.tsx (headerStyle as any on line 112)
    - app/(tabs)/properties/[id].tsx (headerStyle as any on line 63)
    - app/property/[id]/expenses/add.tsx (headerStyle as any on line 82)
    - app/property/[id]/expenses/[expenseId].tsx (headerStyle as any on line 119)
    - app/property/[id]/tenant/[tenantId]/index.tsx (headerStyle as any on line 178, colors: any on line 447)
    - app/property/[id]/tenant/[tenantId]/payment/mark-paid.tsx (headerStyle as any on line 115)
    - app/(tabs)/tools/index.tsx (router.push as any on line 56, icon as any on line 60)
    - app/onboarding/index.tsx (router.replace as any on line 32)
    - app/(auth)/phone-verify.tsx (handleKeyPress e: any on line 76)
    - app/(tabs)/profile/index.tsx (payload.new as any on line 324)
    - app/_layout.tsx (to see established Href import pattern)
  </read_first>
  <action>
**Pattern A: headerStyle as any -> as object (8 files, 8 instances)**

In each file, replace `headerStyle: { backgroundColor: colors.background } as any` with `headerStyle: { backgroundColor: colors.background } as object`.

Similarly replace `headerStyle: { height: 64 } as any` with `headerStyle: { height: 64 } as object`.

The `as object` cast is the narrowest safe cast for Expo Router's headerStyle prop, which expects `AnimatedStyle<ViewStyle>` but accepts plain objects at runtime. This is a known Expo Router typing limitation.

Files:
- app/tools/_layout.tsx:11
- app/notifications/index.tsx:112
- app/(tabs)/properties/[id].tsx:63
- app/property/[id]/expenses/add.tsx:82
- app/property/[id]/expenses/index.tsx:129
- app/property/[id]/expenses/[expenseId].tsx:119
- app/property/[id]/tenant/[tenantId]/index.tsx:178
- app/property/[id]/tenant/[tenantId]/payment/mark-paid.tsx:115

**Pattern B: Router push/replace as any -> as Href (2 files)**

Add `import { type Href } from 'expo-router'` (or add `Href` to existing expo-router import) then:
- app/(tabs)/tools/index.tsx:56 — Change `router.push(tool.route as any)` to `router.push(tool.route as Href)`
- app/onboarding/index.tsx:32 — Change `router.replace(TOUR_STEPS[0].route as any)` to `router.replace(TOUR_STEPS[0].route as Href)`

**Pattern C: Icon name as any -> typed cast (1 file)**
- app/(tabs)/tools/index.tsx:60 — Change `name={tool.icon as any}` to `name={tool.icon as ComponentProps<typeof MaterialCommunityIcons>['name']}` (add `import type { ComponentProps } from 'react'`)

**Fix D: app/(auth)/phone-verify.tsx:76**
Change `function handleKeyPress(e: any, index: number)` to `function handleKeyPress(e: NativeSyntheticEvent<TextInputKeyPressEventData>, index: number)`.
Add import: `import { type NativeSyntheticEvent, type TextInputKeyPressEventData } from 'react-native'` (may already have partial RN imports — add to existing import).

**Fix E: app/(tabs)/profile/index.tsx:324**
Change `const updated = payload.new as any` to `const updated = payload.new as Record<string, unknown>`.
Then update any property accesses from `updated.field` to `updated.field` (should work as-is since Record<string, unknown> allows dynamic access). If specific fields are accessed, read the file to identify them and use appropriate typing.

**Fix F: app/property/[id]/tenant/[tenantId]/index.tsx:447**
Change `{ label: string; value: string; colors: any; last?: boolean }` to `{ label: string; value: string; colors: ReturnType<typeof useTheme>['colors']; last?: boolean }`.
Add `import { useTheme } from '@/lib/theme-context'` if not already imported. Check the file first — if `useTheme` is already imported, just reference the type. Alternatively, if the theme type is complex, use `typeof colors` from the component scope or `Record<string, string>`.

After all changes, run `npx tsc --noEmit` to confirm zero TypeScript errors.
Then run `npm run lint 2>&1 | grep -c "no-explicit-any"` to confirm zero no-explicit-any errors remain.
  </action>
  <verify>
    <automated>cd "C:/Users/Spongeass/Desktop/Claude Code/Dwella v2" && npx tsc --noEmit && npm run lint 2>&1 | grep -c "no-explicit-any"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "as any\|: any" app/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v _layout.tsx` returns 0 matches (note: _layout.tsx was already cleaned in Plan 01)
    - `npm run lint 2>&1 | grep -c "no-explicit-any"` returns 0
    - `npm run lint 2>&1 | grep -c " error "` returns 0 (only warnings remain)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All as any casts in app/ screens are replaced; npm run lint reports zero errors (only warnings); lint can serve as a CI gate</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Verify Sentry production readiness (EDGE-04 gap closure)</name>
  <files>lib/sentry.ts, constants/config.ts, .env.example</files>
  <action>
    This is a human verification checkpoint. The Sentry SDK code infrastructure was fully implemented in Plan 01-02. No code changes are needed. The user must create a Sentry project and add a DSN to the production environment. Present the setup steps and wait for confirmation or deferral.
  </action>
  <verify>
    <automated>cd "C:/Users/Spongeass/Desktop/Claude Code/Dwella v2" && grep -c "EXPO_PUBLIC_SENTRY_DSN" .env.example && grep -c "initSentry" app/_layout.tsx && grep -c "Sentry.init" lib/sentry.ts</automated>
  </verify>
  <what-built>
    Sentry SDK infrastructure was fully implemented in Plan 01-02: lib/sentry.ts initializes crash-only monitoring, constants/config.ts exports SENTRY_DSN, app/_layout.tsx calls initSentry() before SplashScreen. The code no-ops gracefully without a DSN. The only remaining gap is an actual Sentry project and DSN in the production environment.
  </what-built>
  <how-to-verify>
    1. Create a Sentry account at https://sentry.io (if not already done)
    2. Create a new project: Projects -> Create Project -> React Native
    3. Copy the DSN from Project Settings -> Client Keys (DSN)
    4. Add to your .env file: `EXPO_PUBLIC_SENTRY_DSN=https://...@sentry.io/...`
    5. Start the app: `npx expo start`
    6. Optionally verify by temporarily adding `import * as Sentry from '@sentry/react-native'; Sentry.captureException(new Error('test'));` to any screen
    7. Check the Sentry dashboard — the test error should appear within 30 seconds
    8. Remove the test code after verification

    This step is non-blocking. The app works without a DSN. Production crash monitoring requires this setup before the App Store submission.
  </how-to-verify>
  <done>Sentry DSN configured in production environment and test error captured in Sentry dashboard, OR deferred to pre-launch checklist</done>
  <resume-signal>Type "sentry configured" after adding DSN, or "skip" to defer to pre-launch checklist</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
1. `npx tsc --noEmit` exits 0 (compilation baseline preserved)
2. `npm run lint 2>&1 | grep -c " error "` returns 0 (all errors eliminated)
3. `npm run lint 2>&1 | grep -c "no-explicit-any"` returns 0 (all as any casts fixed)
4. `grep -rn "as any" app/ components/ hooks/ lib/ constants/ --include="*.ts" --include="*.tsx" | grep -v node_modules` returns 0 matches
5. ESLint can serve as a clean CI gate (exits with warnings only, not errors)
</verification>

<success_criteria>
- Zero `no-explicit-any` ESLint errors across the entire codebase
- Zero total ESLint errors (only warnings remain)
- `npm run lint` can serve as a CI gate (exit code 0 when no errors, only warnings)
- TypeScript compilation still exits 0
- Sentry DSN setup documented for user action (non-blocking)
</success_criteria>

<output>
After completion, create `.planning/phases/01-compilation-tooling-baseline/01-04-SUMMARY.md`
</output>
